name: boss_plus_cli
description: BOSS 直聘命令行客户端(引用 boss_plus 引擎:登录 / 离线签名 / 通用请求)。
version: 1.0.0
publish_to: 'none'

environment:
  sdk: ^3.11.0

# Add regular dependencies here.
dependencies:
  args: ^2.7.0
  boss_plus:
    path: ../boss_plus
  cookie_jar: ^4.0.9
  path: ^1.9.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
